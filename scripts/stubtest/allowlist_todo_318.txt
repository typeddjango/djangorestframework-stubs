# Allowlist for DRF v3.18 - items not yet covered in stubs

# get_referenced_base_fields_from_q added in 3.18 but not present at runtime
rest_framework.compat.get_referenced_base_fields_from_q

# throttle_scope decorator added in 3.18
rest_framework.decorators.throttle_scope

# SearchFilter.default_lookup attribute added in 3.18
rest_framework.filters.SearchFilter.default_lookup

# UnaccentedSearchFilter added in 3.18
rest_framework.filters.UnaccentedSearchFilter

# NOT_PROVIDED sentinel added to utils.html in 3.18
rest_framework.utils.html.NOT_PROVIDED

# parse_html_dict gained a `default` parameter in 3.18
rest_framework.utils.html.parse_html_dict

# UniqueTogetherValidator.__init__ gained a `nulls_distinct` parameter in 3.18
rest_framework.validators.UniqueTogetherValidator.__init__
